# Allow-Swallow Audit Report

**Generated:** 2026-05-02T16:04:03.075Z
**Total sites:** 43
**Sites missing justification:** 0

> Auto-generated by `scripts/audit-allow-swallow.mjs`. Do not edit by hand.
> Each entry is an intentional, reviewed swallow of a caught error.
> Any new empty `catch {}` without an `allow-swallow:` justification will fail CI via
> `scripts/check-no-swallowed-errors.mjs --strict`.

## All allow-swallow sites

### `src/background/handlers/injection-handler.ts` (3)

| Line | Justification |
|-----:|---------------|
| 507 | settings load failure falls back to default budget |
| 626 | settings load failure falls back to default budget |
| 729 | best-effort IndexedDB cache write |

### `src/background/handlers/injection-wrapper.ts` (1)

| Line | Justification |
|-----:|---------------|
| 50 | preview/test contexts lack chrome.runtime; "0.0.0" sentinel is the documented fallback |

### `src/background/handlers/logging-handler.ts` (1)

| Line | Justification |
|-----:|---------------|
| 283 | startup race — Sessions schema may not yet exist; bg-logger forbidden here (recursion) |

### `src/background/handlers/token-seeder.ts` (3)

| Line | Justification |
|-----:|---------------|
| 191 | localStorage unavailable on this origin; seeding is best-effort |
| 263 | localStorage unavailable; caller falls back to null |
| 497 | malformed URL; fallback regex checks below handle the parse failure |

### `src/background/injection-diagnostics.ts` (4)

| Line | Justification |
|-----:|---------------|
| 53 | diagnostics must never break injection paths |
| 83 | diagnostics must never break injection paths |
| 111 | tab console mirroring is best-effort only |
| 175 | tab pipeline mirroring is best-effort only |

### `src/background/project-db-manager.ts` (4)

| Line | Justification |
|-----:|---------------|
| 181 | ProjectDatabases table may not exist yet if MetaTables schema hasn't run |
| 216 | OPFS unavailable, falls through to chrome.storage |
| 226 | storage failed, falls through to in-memory db |
| 303 | removeEntry is best-effort cleanup |

### `src/background/script-resolver.ts` (1)

| Line | Justification |
|-----:|---------------|
| 75 | cache miss is the expected hot path; logging every miss would flood the console |

### `src/background/service-worker-main.ts` (1)

| Line | Justification |
|-----:|---------------|
| 39 | chrome.action unavailable in test/preview SW contexts; tooltip is cosmetic |

### `src/background/session-log-writer.ts` (4)

| Line | Justification |
|-----:|---------------|
| 467 | missing log file is expected |
| 469 | unreadable session dir is skipped |
| 549 | directory exists but can't be read; report empty file list |
| 555 | OPFS root or session-logs dir doesn't exist |

### `src/components/popup/BootFailureBanner.tsx` (4)

| Line | Justification |
|-----:|---------------|
| 83 | localStorage may be denied; safe default is "full" |
| 122 | clipboard denied; textarea fallback stays visible |
| 132 | clipboard denied; snippet stays visible for manual copy |
| 160 | Blob/URL unavailable in sandbox; Copy report still works |

### `src/components/popup/InjectionCopyButton.tsx` (3)

| Line | Justification |
|-----:|---------------|
| 212 | poll failure is non-critical; next tick retries |
| 233 | extension context invalidated during teardown |
| 259 | extension context already torn down |

### `src/content-scripts/network-reporter.ts` (3)

| Line | Justification |
|-----:|---------------|
| 76 | extension context invalidated mid-flight; report is best-effort |
| 252 | extension context invalidated; status report is best-effort |
| 323 | pagehide flush is best-effort |

### `src/hooks/use-draggable.ts` (3)

| Line | Justification |
|-----:|---------------|
| 52 | corrupt stored position falls back to default |
| 58 | localStorage quota/denied; position is convenience-only |
| 160 | localStorage denied; reset is best-effort |

### `src/lib/click-trail.ts` (4)

| Line | Justification |
|-----:|---------------|
| 58 | sessionStorage may be unavailable (private mode/quota) |
| 67 | clearing diagnostic trail is best-effort |
| 120 | clearing frozen trails is best-effort |
| 140 | snapshot eviction is best-effort |

### `standalone-scripts/marco-sdk/src/notify.ts` (4)

| Line | Justification |
|-----:|---------------|
| 138 | missing theme tokens fall back to defaults |
| 185 | listener errors must not break notify pipeline |
| 295 | execCommand fallback for old browsers; UI continues regardless |
| 331 | stop-loop callback errors must not block toast pipeline |

